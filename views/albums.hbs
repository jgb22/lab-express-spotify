<!DOCTYPE html>
<html lang="en">
<head>
 <title>Albums results</title>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"></head>
<body>
    <h1>Albums</h1>

    {{#if albums.length}}
        <ul>
            {{#each albums}}
                <li>
                    <h3>{{name}}</h3>
                    <img src="{{images.[0].url}}" alt="Album Cover">
                    <a href="/tracks/{{id}}">View Tracks</a> <!-- Link to view tracks for a particular album -->
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No albums found</p>
    {{/if}}
</body>
</html>
